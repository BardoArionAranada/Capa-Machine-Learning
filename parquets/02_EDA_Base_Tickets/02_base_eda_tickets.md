# 02 Base EDA Tickets

## Proposito de esta etapa

Esta carpeta se reserva para la base que se use durante el analisis exploratorio.

## Nota actual

En esta etapa se utilizara el mismo `Parquet` principal generado en:

- `parquets/01_Carga_y_Validacion_Parquet/01_base_tickets_modelado.parquet`

Si mas adelante se genera una version derivada para EDA, se colocara aqui.
